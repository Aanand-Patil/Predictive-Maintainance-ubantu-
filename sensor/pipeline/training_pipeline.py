from sensor.logger import logging
from sensor.exception import SensorException
import sys, os


def start_traing_pipeline():
    try:
        pass
    except Exception as e:
        raise SensorException(e, sys)
